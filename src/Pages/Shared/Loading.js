import React from 'react';

const Loading = () => {
    return (
        <div className='w-full h-screen flex justify-center items-center'>
            <h2 className='text-6xl text-secondary'>Loading...</h2>
        </div>
    );
};

export default Loading;